apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: payment-risk, namespace: payment-risk-rust }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: app, app.kubernetes.io/part-of: payment-risk } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
