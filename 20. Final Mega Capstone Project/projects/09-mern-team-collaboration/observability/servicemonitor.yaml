apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: collaboration, namespace: collaboration-mern }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: api, app.kubernetes.io/part-of: collaboration } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
