apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: helpdesk, namespace: helpdesk-node }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: app, app.kubernetes.io/part-of: helpdesk } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
