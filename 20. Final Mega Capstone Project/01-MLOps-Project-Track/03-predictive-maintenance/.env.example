# Native optional overrides; load them in your shell if you need custom storage.
# The CLI defaults to ./state inside the project and ../state/telemetry.db.
# Compose sets its own container paths in the Dockerfile.
# STATE_DIR=/absolute/path/to/project-state
# TELEMETRY_DB=/absolute/path/to/shared/telemetry.db
