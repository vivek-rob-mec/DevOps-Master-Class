apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: saas, namespace: saas-typescript }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: api, app.kubernetes.io/part-of: saas } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
