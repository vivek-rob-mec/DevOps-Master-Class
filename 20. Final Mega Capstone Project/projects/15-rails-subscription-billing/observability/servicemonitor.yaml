apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: billing, namespace: billing-rails }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: app, app.kubernetes.io/part-of: billing } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
