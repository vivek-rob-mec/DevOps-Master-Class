---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: collaboration-api
  namespace: collaboration-mern
  labels: { app.kubernetes.io/name: api, app.kubernetes.io/part-of: collaboration }
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy: { rollingUpdate: { maxUnavailable: 0, maxSurge: 1 } }
  selector:
    matchLabels: { app.kubernetes.io/name: api, app.kubernetes.io/part-of: collaboration }
  template:
    metadata:
      labels: { app.kubernetes.io/name: api, app.kubernetes.io/part-of: collaboration }
    spec:
      serviceAccountName: collaboration
      automountServiceAccountToken: false
      securityContext: { seccompProfile: { type: RuntimeDefault } }
      terminationGracePeriodSeconds: 30
      containers:
        - name: api
          image: ghcr.io/replace-me/09-mern-team-collaboration-api:0.1.0
          imagePullPolicy: IfNotPresent
          ports: [{ name: http, containerPort: 4000 }]
          env: [{ name: PORT, value: "4000" }]
          envFrom:
            - configMapRef: { name: collaboration-config }
            - secretRef: { name: collaboration-runtime }
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 5, periodSeconds: 5, timeoutSeconds: 2, failureThreshold: 6 }
          livenessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 20, periodSeconds: 10, timeoutSeconds: 2 }
          lifecycle: { preStop: { exec: { command: ["/bin/sh", "-c", "sleep 5"] } } }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts: [{ name: tmp, mountPath: /tmp }]
      volumes: [{ name: tmp, emptyDir: { sizeLimit: 128Mi } }]
---
apiVersion: v1
kind: Service
metadata:
  name: collaboration-api
  namespace: collaboration-mern
  labels: { app.kubernetes.io/name: api, app.kubernetes.io/part-of: collaboration }
spec:
  selector: { app.kubernetes.io/name: api, app.kubernetes.io/part-of: collaboration }
  ports: [{ name: http, port: 80, targetPort: http }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: collaboration-frontend
  namespace: collaboration-mern
  labels: { app.kubernetes.io/name: frontend, app.kubernetes.io/part-of: collaboration }
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy: { rollingUpdate: { maxUnavailable: 0, maxSurge: 1 } }
  selector:
    matchLabels: { app.kubernetes.io/name: frontend, app.kubernetes.io/part-of: collaboration }
  template:
    metadata:
      labels: { app.kubernetes.io/name: frontend, app.kubernetes.io/part-of: collaboration }
    spec:
      serviceAccountName: collaboration
      automountServiceAccountToken: false
      securityContext: { seccompProfile: { type: RuntimeDefault } }
      terminationGracePeriodSeconds: 30
      containers:
        - name: frontend
          image: ghcr.io/replace-me/09-mern-team-collaboration-frontend:0.1.0
          imagePullPolicy: IfNotPresent
          ports: [{ name: http, containerPort: 8080 }]
          env: [{ name: PORT, value: "8080" }]
          envFrom:
            - configMapRef: { name: collaboration-config }
            - secretRef: { name: collaboration-runtime }
          readinessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 5, periodSeconds: 5, timeoutSeconds: 2, failureThreshold: 6 }
          livenessProbe: { httpGet: { path: /health, port: http }, initialDelaySeconds: 20, periodSeconds: 10, timeoutSeconds: 2 }
          lifecycle: { preStop: { exec: { command: ["/bin/sh", "-c", "sleep 5"] } } }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts: [{ name: tmp, mountPath: /tmp }]
      volumes: [{ name: tmp, emptyDir: { sizeLimit: 128Mi } }]
---
apiVersion: v1
kind: Service
metadata:
  name: collaboration-frontend
  namespace: collaboration-mern
  labels: { app.kubernetes.io/name: frontend, app.kubernetes.io/part-of: collaboration }
spec:
  selector: { app.kubernetes.io/name: frontend, app.kubernetes.io/part-of: collaboration }
  ports: [{ name: http, port: 80, targetPort: http }]
