image:
  registry: ghcr.io
  owner: replace-me
  prefix: 07-python-learning-monolith
  tag: 0.1.0
  pullPolicy: IfNotPresent
existingSecret: ""
config: { APP_ENV: production, LOG_LEVEL: info }
workloads:
  - { name: app, port: 8000, health: /health, replicas: 2 }
ingress: { enabled: true, className: nginx, host: learning.example.com }
autoscaling: { enabled: true, min: 2, max: 6, cpu: 70 }
