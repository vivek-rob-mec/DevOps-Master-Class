server { listen 8080; root /usr/share/nginx/html/browser; location = /health { default_type application/json; return 200 '{"status":"ok","service":"frontend"}'; } location /api/ { proxy_pass ${API_URL}/api/; } location / { try_files $uri /index.html; } }
