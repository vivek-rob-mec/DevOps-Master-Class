apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: claims, namespace: claims-dotnet }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: app, app.kubernetes.io/part-of: claims } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
