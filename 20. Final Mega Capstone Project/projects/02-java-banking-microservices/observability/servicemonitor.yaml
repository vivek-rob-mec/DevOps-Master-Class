apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: banking, namespace: banking-java }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: gateway, app.kubernetes.io/part-of: banking } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
