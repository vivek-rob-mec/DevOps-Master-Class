apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: shortener, namespace: shortener-go }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: app, app.kubernetes.io/part-of: shortener } }
  endpoints: [{ port: http, path: /metrics, interval: 30s, scrapeTimeout: 10s }]
